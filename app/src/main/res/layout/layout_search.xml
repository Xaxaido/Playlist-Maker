<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content" >

    <LinearLayout
        android:id="@+id/search_box"
        android:layout_width="0dp"
        android:layout_height="36dp"
        android:layout_marginStart="@dimen/padding_small_8x"
        android:layout_marginTop="@dimen/padding_small_4x"
        android:layout_marginEnd="@dimen/padding_small_8x"
        android:layout_marginBottom="@dimen/padding_small_4x"
        android:background="@drawable/bg_search_box"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/search_icon"
            android:layout_width="18dp"
            android:layout_height="16dp"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="@dimen/padding_small_6x"
            android:layout_marginTop="@dimen/padding_small_5x"
            android:layout_marginBottom="@dimen/padding_small_5x"
            android:src="@drawable/ic_menu_search"
            app:tint="@color/inactive_button_white_bg"
            tools:ignore="ContentDescription" />

        <EditText
            android:id="@+id/search_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/padding_small_4x"
            android:layout_gravity="center_vertical"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/search_hint"
            android:autofillHints="true"
            android:textColorHint="@color/inactive_button_white_bg"
            android:inputType="text"
            android:textColor="@color/button_text"
            android:textSize="@dimen/text_normal_size"
            tools:ignore="LabelFor" />

        <ImageButton
            android:id="@+id/button_clear"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:paddingHorizontal="@dimen/padding_small_6x"
            android:paddingVertical="@dimen/padding_small_5x"
            android:background="@null"
            android:foreground="?selectableItemBackground"
            android:src="@drawable/ic_close"
            android:visibility="gone"
            app:tint="@color/inactive_button_white_bg"
            tools:ignore="ContentDescription"
            tools:visibility="visible" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>